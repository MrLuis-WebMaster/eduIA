# Infrastructure

Learning-progress has no local driven adapters.

It receives tutoring history through the application port `ListRecentSessions`
(anti-corruption layer). Persistence stays in the tutoring module.
