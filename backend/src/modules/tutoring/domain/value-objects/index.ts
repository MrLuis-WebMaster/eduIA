/**
 * Tutor profile value objects (closed unions).
 * Session persistence lives on the device (ADR 0001); this context is response generation.
 */
export type {
  Difficulty,
  UserRole,
  ExplanationStyle,
  TutorPersonality,
  ConversationRole,
  ConversationMessage,
  TutorRequest,
  TutorResponse,
} from '../types.js';
